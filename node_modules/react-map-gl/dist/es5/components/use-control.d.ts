/// <reference types="mapbox-gl" />
import type { IControl, ControlPosition } from '../types';
import type { MapContextValue } from './map';
declare type ControlOptions = {
    position?: ControlPosition;
};
export default function useControl(onCreate: (context: MapContextValue) => IControl, onRemove?: ((context: MapContextValue) => void) | ControlOptions, opts?: ControlOptions): IControl;
export {};
